import { ChatScreen } from '../../src/features/chat/ChatScreen';

/** Fresh chat — session key is created asynchronously by bootstrap. */
export default function ChatIndexRoute() {
  return <ChatScreen />;
}
