/**
 * Notes tab — notes list (reuses NotesScreen adapted for tab context).
 */
import { NotesTabScreen } from '../../src/features/home/NotesTabScreen';

export default function NotesTabRoute() {
  return <NotesTabScreen />;
}
