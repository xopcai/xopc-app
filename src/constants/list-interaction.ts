/** Global long-press delay for list multi-select (ms). */
export const LIST_DELAY_LONG_PRESS = 300;

/** Undo window for single-item delete snackbars (ms). */
export const LIST_DELETE_UNDO_MS = 5000;
