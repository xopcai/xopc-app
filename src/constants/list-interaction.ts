/** Long-press delay for list item selection — global per AGENTS.md */
export const LIST_DELAY_LONG_PRESS = 300;

/** Snackbar undo window for single-item delete */
export const LIST_DELETE_UNDO_MS = 5000;
