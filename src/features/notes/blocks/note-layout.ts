export const NOTE_DETAIL_HEADER_BODY = 60;
export const VIEW_BOTTOM_BAR_HEIGHT = 64;
/** Height of the keyboard-attached block insert bar: 44px chips + 8px vertical padding on each side. */
export const INSERT_BAR_HEIGHT = 60;
/** Horizontal inset for note title + block editor rows (aligned). */
export const NOTE_EDITOR_HORIZONTAL_INSET = 12;
